stages:
  prepare:
    cmd: ./convert_imgs.sh && python3 prepare.py
    deps:
      - data/raw
    outs:
      - data/train
      - data/test

  train:
    cmd: python3 train.py
    deps:
      - data/train
      - data/test
    outs:
      - log/models
